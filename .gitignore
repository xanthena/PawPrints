cat-videos.zip

# secrets
.env

# python
__pycache__/
*.pyc
venv/
.venv/

# node / electron
node_modules/
src/frontend/dist/
src/frontend/build/

# local-only test data -- videos, frames, jsons, pet profiles, uploads,
# and anything else generated locally must never be pushed. Keep the
# folder (and link.txt) but ignore everything inside it.
src/data/*
!src/data/link.txt
!src/data/frames/
src/data/frames/*
!src/data/frames/.gitkeep

# local-only run output -- never pushed, only used for local testing
src/results/*
!src/results/.gitkeep

# md files
AGENTS.md
changes.md
integration_issues.md
security_risks.md

# claude code local tooling, not part of the shared project
.claude/
