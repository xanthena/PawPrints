// Placeholder data until the backend can list processed footage and
// generate real thumbnails from each day's timeline. Left empty so the
// dashboard starts clean -- only real uploads populate it now.
export const MOCK_FOOTAGES = []
