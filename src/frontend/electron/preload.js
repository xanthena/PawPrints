// IPC bridge between the renderer (React UI) and the backend/filesystem
// will be exposed here once the upload flow needs to talk to anything
// outside the browser sandbox (e.g. saving the picked file for the
// FastAPI pipeline to process).
